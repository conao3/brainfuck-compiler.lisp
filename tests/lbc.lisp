(defpackage lbc-test
  (:use :cl
        :lbc
        :prove))
(in-package :lbc-test)

;; NOTE: To run this test file, execute `(asdf:test-system :lbc)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
